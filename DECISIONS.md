# DECISIONS

## Audit Notes
- [AUDIT] Decision: Treat `docs/parity/PARITY_ATTACK_PLAN.md` as the checklist source for one-time audit seeding into root `ATTACK_PLAN.md`. | Why: root attack/decision files did not exist; parity plan already contains the active checklist and completion markers. | Alternative: create a brand-new checklist from scratch and ignore existing parity documentation.
- [AUDIT] Decision: Keep all currently `[x]` items as complete after static verification rather than downgrading any. | Why: each audited item has concrete implementation evidence in Godot code/files; no direct code contradiction found. | Alternative: conservatively downgrade items without runtime execution evidence.
- [VALIDATION] Decision: Log headless automation as blocked instead of claiming full parse validation. | Why: terminal context cannot run `godot` (`command not found`), so `--headless --check-only` cannot be executed here. | Alternative: mark validation complete based only on editor/manual assumptions.

## Session Decisions
- [BOOT/PARSE] Decision: Convert constructor-based constants in `main.gd` to runtime vars for compatibility (`PackedStringArray(...)`, `Color(...)`). | Why: current parser raised constant-expression errors. | Alternative: keep constants and require a different Godot parser/runtime behavior.
- [ASTEROIDS] Decision: Define `variant` in `AsteroidSystem.update_asteroids()` from asteroid payload before use. | Why: code referenced `variant` before declaration, causing parse/runtime failure. | Alternative: inline repeated dictionary lookups at each branch.

- [ATTACK_PLAN] Decision: Resolve duplicated root `ATTACK_PLAN.md` sections into a single canonical structure. | Why: file had contradictory duplicate blocks from prior sessions; a single source reduces ambiguity for queued runs. | Alternative: keep both blocks and infer latest state heuristically each session.
- [CAMERA D.5] Decision: Mirror web `updateCamera` movement constants in Godot (`speed=38`, y clamp `6..140`, x/z clamp `±170`). | Why: current Godot camera movement was slower and more constrained than web source-of-truth values. | Alternative: keep current Godot tuning and treat camera handling as an approved difference.
- [CAMERA D.5] Decision: Clamp per-frame mouse look deltas to `±220` before applying sensitivity. | Why: web clamps pointer deltas to avoid focus/pointer-lock jump spikes; Godot behavior should match for camera stability. | Alternative: leave unclamped and rely on platform input smoothness.
- [CAMERA D.5] Decision: Align CameraSystem startup/reset pose to web (`camPos(0,55,70)`, `yaw=PI`, `pitch=-0.65`). | Why: Godot defaults were materially different and changed initial framing/orientation vs source-of-truth behavior. | Alternative: keep existing Godot defaults as a prototype-specific camera framing.
- [POINTER D.2] Decision: Gate PLAYING camera look and play-click actions on active mouse capture, and consume first click after focus-out as recapture-only. | Why: web behavior relocks before gameplay actions; without gating, Godot can process unintended build/sell/look on relock transitions. | Alternative: always process clicks/looks immediately after focus return.
- [MENU D.3] Decision: Make menu virtual-cursor hit testing iterate controls in reverse (top-most first) and skip disabled/invisible buttons. | Why: web `pickMenuHitTarget` effectively clicks the top-most eligible element under cursor; forward-order scan could select non-topmost or disabled targets. | Alternative: preserve fixed-order scan and allow disabled/highly obscured controls to be highlighted.
- [MENU D.3] Decision: Set Godot menu virtual-cursor clamp margin to `14` to match web `MENU_CURSOR_MARGIN`. | Why: previous Godot margin (`8`) allowed cursor positions web UI never permits, creating subtle menu navigation drift near edges. | Alternative: keep smaller Godot-specific margin for looser movement.
- [ASTEROIDS F.7] Decision: Feed seeker asteroids a live target-point list and retarget them to the closest structure each update. | Why: web seekers explicitly steer toward closest live building instead of flying only on initial centerline direction. | Alternative: keep seeker trajectory as command-center-only vector for simpler movement.
- [ASTEROIDS F.6/F.7] Decision: Move spawner meteor cadence from fixed reset (`6.0s`) to web-scaled cooldown (`max(2.2, 5.2 - adj*0.12)`). | Why: web spawner pressure ramps with wave scaling; fixed cooldown under-represents late-wave threat behavior. | Alternative: preserve fixed interval for simpler tuning.
- [ASTEROIDS F.8] Decision: Trigger asteroid impacts via near-target threshold (`2.2`) / low altitude check instead of using `impact_radius` as trigger distance. | Why: web uses a fixed impact trigger and applies `impactRadius` only for resulting AOE damage; using radius as trigger detonates too early for large-radius variants. | Alternative: continue radius-based trigger for simpler single-value behavior.
- [ASTEROIDS F.6] Decision: Replace placeholder variant picker with web-style wave-ramped variant weights and capped active-type pool (`2 + floor(wave/3)`, always including `normal`). | Why: uniform/random placeholder selection did not match web pacing or composition pressure over waves. | Alternative: keep simplified random variant selection for prototype readability.
- [ASTEROIDS F.6] Decision: Set `colossus` visual size multiplier to `10.0` to match web spawn sizing. | Why: lower Godot value significantly underrepresented colossus screen presence and perceived threat. | Alternative: keep reduced size as a prototype readability compromise.
- [ASTEROIDS F.7] Decision: Assign seeker target at spawn to the closest live structure, not just during subsequent movement ticks. | Why: web seeker spawn logic chooses nearest building immediately, so first-frame heading should already reflect target acquisition. | Alternative: rely only on per-frame retargeting after spawn.
- [ASTEROIDS F.6/F.9] Decision: Add run-scoped asteroid discovery tracking with a 5-second HUD notice and audio event on first sight of each variant. | Why: web tracks discovered variants and surfaces discovery feedback; without this, new variant introductions are silent and less readable. | Alternative: keep discovery implicit with no explicit notification system.
- [ASTEROIDS F.6] Decision: Add discovery-aware variant pool construction (known-first + keep `normal` + optional unknown intro from wave 2). | Why: web uses discovered history to shape each wave’s variant pool, not just raw global weighting. | Alternative: continue using only weighted global pool independent of discovery state.
- [ASTEROIDS F.6] Decision: Cache and reuse a single variant pool per wave, rebuilding only when wave index changes. | Why: web configures `waveVariantPool` at wave start; per-spawn pool recompute can shift composition mid-wave in ways web does not. | Alternative: continue recomputing variant pool every spawn using latest discovery set.
- [ASTEROIDS F.9] Decision: Map discovered variant ids to user-facing names in HUD discovery text. | Why: web discovery toast presents readable variant names; raw ids are less legible and less parity-aligned for UX. | Alternative: show raw id strings directly.
- [ASTEROIDS F.9] Decision: Include discovery description text in HUD notice alongside variant name. | Why: web discovery payload includes `name` + `description`; name-only notice drops useful context when new variants appear. | Alternative: keep compact name-only discovery notice.
- [ASTEROIDS F.7] Decision: Apply `stasisTimer` and `pulsarSlowTimer` movement effects in `update_asteroids` (`freeze` and `0.52` speed multiplier). | Why: web movement loop applies these timers directly; ignoring them blocks parity for Nova slow/stasis interactions. | Alternative: postpone timer handling until full status-effect feature pass.
- [ASTEROIDS F.7] Decision: Decay `radarMarkTimer` in asteroid update loop alongside other status timers. | Why: web decrements this timer continuously; leaving it static breaks duration semantics for mark-dependent effects. | Alternative: handle radar mark decay in a separate subsystem later.
- [ASTEROIDS F.7/F.8] Decision: Use planar XZ distance (not full 3D) for seeker nearest-target selection, impact trigger proximity, and asteroid death AOE/EMP range checks. | Why: web source uses `Math.hypot(x,z)` grid distance for these behaviors; including Y introduced false range differences due to height offsets. | Alternative: keep 3D distance checks and tolerate altitude-influenced behavior drift.
- [ASTEROIDS F.7/F.8] Decision: Drive non-seeker asteroid movement/impact off each asteroid's `target` point and center impact AOE/EMP on that point for impact deaths. | Why: web asteroids carry a per-spawn target and detonate relative to that target; command-center-only targeting compressed impact distribution and altered defense interaction patterns. | Alternative: keep command-center-anchored movement/impact for all non-seekers.
- [ASTEROIDS F.6/F.7] Decision: Generate randomized non-seeker target points in web board range (`[-40, 40]` X/Z) and pass parent target into spawned meteors/splitter children. | Why: web non-seekers aim at varied grid coordinates and child/spawner asteroids keep the parent impact lane; recomputing fresh targets changed lane continuity and spread. | Alternative: keep command-center/random recompute targeting per child spawn.
- [ASTEROIDS F.6/F.7] Decision: Match base asteroid spawn geometry to web radial spawn (`r=160..220`, `y=90..120`) instead of low side-edge spawn. | Why: web spawn lane and time-to-impact depend on high-altitude radial entry; edge-at-ground spawning compresses travel time and changes early-wave pressure. | Alternative: keep side-edge low spawn for simpler arena-local behavior.
- [ASTEROIDS F.6/F.7] Decision: Spawn spawner-created meteors with web-like child derivation from parent stats (`speed=max(26,parent*1.45)`, `hp=max(45,parentMaxHp*0.42)`, `impactDamage=parent*0.8`, `impactRadius=0.95`) and close local offset. | Why: generic meteor spawn kinematics over/under-shot child threat relative to web spawner behavior and broke expected burst profile. | Alternative: keep generic `_spawn_asteroid_at("meteor")` stats for simplicity.
- [ASTEROIDS F.6/F.7] Decision: Spawn splitter children using web side-based local offsets (`(-0.55,0.2,-0.25)` / `(0.55,0.2,0.25)` pattern) instead of wide random offsets. | Why: web splitter behavior creates a readable left/right split lane; broad random offsets changed immediate child clustering and visual trajectory consistency. | Alternative: keep random child offsets for variation.
- [ASTEROIDS F.6/F.7] Decision: Apply web-like splitter lane divergence by rotating child heading relative to parent lane (`±0.35` / `±0.42` by split level) and deriving child target from that rotated direction. | Why: web splitter children diverge by yaw from parent velocity; shared parent target caused both children to collapse back into the same lane. | Alternative: keep identical inherited target for both children.
- [AUDIO K.2] Decision: Extend `AudioService` optional event map to include web event ids `upgrade_refund` and `shield_hit`. | Why: web emits these event types; silently dropping them in Godot makes future parity callsites no-op even when SFX assets exist. | Alternative: keep only currently-used event ids and add others later ad hoc.
- [AUDIO K.2] Decision: Emit `asteroid_destroyed` reason-based audio from `_remove_asteroid` (centralized), not projectile kill callsites. | Why: web resolves destroy audio in a single death handler by reason (`combat`/`shield` vs `impact`); centralization prevents callsite drift and missing sound when new kill paths are added. | Alternative: keep per-caller emission and manually duplicate reason logic.
- [AUDIO K.2] Decision: Mirror web `gameAudioEngine` semantics for `asteroid_destroyed`/`shield_hit`: destroy SFX only for `reason=="combat"` and throttle `shield_hit` to 72ms minimum interval. | Why: web explicitly suppresses shield-destroy destroy SFX and rate-limits shield-hit spam; unconditional playback overstates shield interactions. | Alternative: keep generic per-event playback with no cooldown.
- [AUDIO K.2] Decision: Keep `wave_start` / `wave_cleared` audio events silent by removing optional SFX mapping in Godot. | Why: web event handler explicitly treats these as no-op markers; playing SFX on wave edges introduces non-parity sound cues. | Alternative: keep whoosh/door optional mapping for added feedback.
- [AUDIO K.2] Decision: Use a dedicated short-lived player for `build_sell` and stop playback at ~72ms to mirror web clip behavior. | Why: web explicitly truncates door-close playback for sells; full-length playback in Godot overemphasized sell actions and diverged from source cadence. | Alternative: play full `build_sell` clip via shared SFX player.
- [ASTEROIDS F.6] Decision: Remove spawner meteor spawn-on-death from `on_asteroid_destroyed`; keep spawner behavior limited to periodic spawn while alive. | Why: web `handleAsteroidDeath` does not create extra meteor on spawner death; retaining it inflated post-kill pressure beyond source behavior. | Alternative: keep death spawn as a Godot-only difficulty modifier.
- [ASTEROIDS F.6] Decision: Remove colossus death AOE from `on_asteroid_destroyed`; keep colossus threat in spawn stats/impact only. | Why: web death handler has no special colossus death blast; Godot-only death AOE added non-source damage spikes when large enemies die. | Alternative: preserve colossus death blast as custom Godot difficulty flavor.
- [ASTEROIDS F.6 / ECON H.4] Decision: Replace EMP death turret-disable effect with web-style power drain (`radius=18`, `22 * POWER_DRAIN_GLOBAL_MUL` per building hit, including command center). | Why: web EMP death path drains stored power by nearby-building count and does not apply turret disable timers; disable behavior skewed defense uptime. | Alternative: keep turret-disable EMP as a simplified crowd-control proxy.
- [UPGRADES I.2] Decision: Implement same-inactive-phase upgrade refund for prototype slots by re-pressing the same upgrade key, including current-phase prereq invalidation cleanup. | Why: web exposes refundable upgrades during inactive phase and auto-refunds now-invalid current-phase dependents; buy-only flow diverged from phase/refund semantics. | Alternative: postpone refunds until full upgrade tree UI exists.
- [UPGRADES I.2 / UI J.3] Decision: Mark owned research labels with `[R]` when refundable in current inactive phase and not in wave combat. | Why: web state exposes `refundableUpgradeIds`; without a marker, same-key refund capability is hidden/discoverability is poor. | Alternative: keep refund available but undocumented in label text.
- [UPGRADES I.2] Decision: Add explicit `wave_combat_active` guards inside `UpgradeSystem.try_buy_upgrade` and `try_refund_upgrade` instead of relying solely on caller-side input gating. | Why: web semantics forbid phase transactions during active waves; system-level guards prevent future callsites from bypassing this rule. | Alternative: keep gating only in `main.gd` input path.
- [UPGRADES I.2 / UX D.4] Decision: Normalize `build_mode` to `turret` after upgrade state changes when current mode becomes locked due to refund/auto-refund. | Why: web recomputes unlocked selection after refund; keeping stale locked build mode in Godot causes dead input state until manual cycle. | Alternative: allow stale mode and rely on failed placement attempts.
- [ARCH A.2 / UPGRADES I.2] Decision: Add `upgrade_*_phase` fields to `GameState` and sync them from `main.gd` runtime state. | Why: refund parity now depends on phase ownership metadata; omitting these fields from state mirror leaves `GameState` incomplete for downstream systems/debug tooling. | Alternative: keep phase metadata only in `main.gd` locals.
- [UI J.3 / UPGRADES I.2] Decision: Surface prototype upgrade refund availability in the gameplay HUD hint whenever same-phase refunds are possible. | Why: web exposes refundable upgrade state continuously; Godot previously had refund logic but no in-run affordance outside research labels. | Alternative: rely only on `[R]` markers in research labels and keep gameplay HUD unchanged.
- [UI J.3 / UPGRADES I.2] Decision: Change refundable owned research marker from `[R]` to `[$]` in Godot labels. | Why: web skill nodes indicate refundability with a dollar affordance, so a dollar marker is a closer parity cue than a letter mnemonic. | Alternative: keep `[R]` to emphasize keyboard action over icon parity.
- [UI J.3 / UPGRADES I.2] Decision: Update research panel hint text to describe buy/refund key behavior and `[$]` marker semantics. | Why: once marker and refund flow matched web semantics, panel copy needed to explain the affordance so behavior is discoverable in-run. | Alternative: leave existing buy-only hint and rely on HUD/label context alone.
- [INPUT D.4 / UI J.3] Decision: Add dedicated `toggle_research_panel` action on `R` and gate research panel visibility through an explicit `research_panel_open` state. | Why: web has explicit research overlay open/close semantics, while always-on panel behavior in Godot prevents parity-level shortcut flow. | Alternative: keep panel permanently visible and only improve text hints.
- [INPUT D.4] Decision: Add `C` as an alternate build-mode cycle action while retaining existing `B` binding. | Why: web shell uses `C` for build UI flow; adding an alias improves shortcut parity without breaking existing Godot controls. | Alternative: replace `B` outright and risk regressions for current testers.
- [UI D.4 / J.3] Decision: Update research panel + HUD hint text to explicitly advertise `R` panel toggle and `B/C` build shortcuts. | Why: newly added shortcut parity is not useful unless surfaced in visible gameplay copy, mirroring web-style discoverable control hints. | Alternative: keep hidden shortcuts and rely on external documentation.
- [TRACKING A.2 / UI J.3] Decision: Normalize root `ATTACK_PLAN.md` refund-marker wording from stale `[R]` to `[$]`. | Why: plan text must reflect actual shipped behavior to avoid future contradictory session choices. | Alternative: leave stale note and rely on commit history for correctness.
- [UI D.4 / J.3] Decision: Set `research_panel_open` default/reset state to closed (`false`) at run start. | Why: web overlay state initializes closed and only opens on explicit shortcut, so always-open Godot startup behavior was non-parity. | Alternative: keep overlay open by default to maximize immediate information density.
- [UI D.4 / J.3] Decision: Restrict research overlay visibility/toggle handling to PLAYING only and force-close it on phase transitions out of gameplay. | Why: web overlay interactions are gameplay-context UI; keeping it active through PAUSED/menu/gameover creates lifecycle drift and stale-open states. | Alternative: allow overlay in PAUSED for convenience and preserve last-open state across phases.
- [UI J.2] Decision: Add a dedicated gameplay `DiscoveryToast` label for first-time asteroid variant notices. | Why: web uses a separate discovery toast presentation; folding discovery only into the long gameplay info string reduced visibility and parity. | Alternative: keep discovery notices embedded only in `GameplayInfo`.
- [UI J.2] Decision: Remove duplicated inline discovery suffix from `GameplayInfo` once dedicated toast exists. | Why: showing the same discovery message in both the toast and long HUD line created noisy duplicate UI that web does not have. | Alternative: keep duplicate messaging for redundancy.
- [UI J.2] Decision: Tint `DiscoveryToast` using `AsteroidSystem.variant_color` keyed by discovered variant id. | Why: web discovery toast includes a variant color chip; applying variant color tint in Godot restores that immediate visual classification cue. | Alternative: keep neutral toast color for simpler styling.
- [UI J.2] Decision: Replace single-line discovery toast label with a panelized title/body layout. | Why: web discovery notice separates heading and description, and packed single-label text was less readable as descriptions grow. | Alternative: keep one multiline label with manual string formatting.
- [UI J.2] Decision: Use an in-toast color swatch node (`DiscoveryToastSwatch`) and keep panel tint neutral. | Why: web toast uses a localized colored icon/chip; tinting the whole panel over-applied color and reduced text contrast. | Alternative: continue tinting the entire toast container.
- [UI J.2] Decision: Centralize discovery toast rendering in `_update_discovery_toast()` and clear body/title when hidden. | Why: phase transitions and timer expiry both affect discovery visibility; single-point update prevents stale text and diverging callsite behavior. | Alternative: keep ad-hoc per-callsite visibility/text updates.
- [UI J.2] Decision: Add a dedicated `GameOverStats` label fed by run metrics (earned/spent/power/kills) in `_finalize_run_score()`. | Why: web game-over UI shows a distinct stats block; relying only on hint text + score omitted this structured post-run summary. | Alternative: keep single-line gameover hint with no separate stats block.
- [UI J.2] Decision: Format `GameOverStats` as two grouped rows instead of one long row. | Why: web presents post-run stats as grouped fields; wrapping into two lines improves scanability and keeps label width stable on smaller windows. | Alternative: keep a single long stats line.
- [UI J.2] Decision: Simplify `GameOverHint` to run-context text (wave/commander) and leave numeric fields in `GameOverStats`. | Why: duplicating earned/spent/kills in both hint and stats produced noisy repetition; web separates context line from stats block. | Alternative: keep full metrics in hint alongside stats.
- [UI J.2] Decision: Track building placement counts during run and append `Most common building` to game-over stats. | Why: web post-run stats explicitly include most common building, which was missing from Godot's summary. | Alternative: omit this field until a fuller end-screen redesign.
- [UI J.2] Decision: Align game-over copy to web wording (`Waves survived`, `Money earned/spent`, `Power produced`, `Asteroids killed`). | Why: even when values matched, non-parity labels made the end-screen read differently from the source UI. | Alternative: keep shorter Godot-only shorthand labels.
- [UI J.2] Decision: Match sandbox hint copy to web (`Sandbox — score not saved`) including dash punctuation. | Why: the previous parenthetical wording was semantically close but not text-parity with the source game-over line. | Alternative: keep existing `Sandbox (score not saved)` note.
- [UI J.3] Decision: Continue calling `_update_hud()` each frame during `PAUSED` to keep research affordability/prereq label suffixes live. | Why: parity plan requires research line state to stay current in both PLAYING and PAUSED HUD flow; phase-transition-only updates can drift from that behavior. | Alternative: update labels only when entering PAUSED.
- [AUDIO K.2] Decision: Route `main.gd` volume slider changes into `AudioService.set_master_volume()` and apply the value to both SFX players. | Why: web uses a global master-volume gain (`Howler.volume`); without propagation, Godot UI volume percent did not affect actual playback loudness. | Alternative: keep UI-only volume text until full audio-bus implementation.
- [AUDIO K.2] Decision: For `asteroid_destroyed`, play small SFX for non-combat reasons and reserve large clip only for combat colossus/planet kills. | Why: web audio handler falls back to small destroy sound in non-combat branches instead of silence. | Alternative: keep non-combat destroys muted.
- [UI D.4] Decision: Replace `MenuHint` text with web-equivalent control guidance copy. | Why: concise Godot-only hint omitted several web-documented shortcuts and reduced UX parity at first-run onboarding. | Alternative: keep short generic virtual-cursor hint.
- [UI J.1/J.3] Decision: Expand gameplay HUD tail shortcut text to include `C build`, `U skills`, `R panel`, `P pause`. | Why: web shell keeps core shortcuts visible during play, while terse Godot text hid build/research entry points. | Alternative: keep minimal `R panel`/pause hint only.
- [UI D.4/J.1] Decision: Update `look_readout` shortcut segment to web-style terms (`C wheel`, `U skills`, `R research`, `RMB sell`). | Why: the previous `B/C toggle` wording reflected prototype behavior but did not match the web-facing control language shown to players. | Alternative: leave legacy `B/C toggle` string.
- [UI J.1] Decision: Normalize gameplay spawn-status copy to web wave-status phrases (`Press Space to start`, `Spawning asteroids...`, `Asteroids remaining`, `Next wave in ... (Space)`). | Why: prior Godot text mixed prototype-specific terms (`window %`, `cleanup`) that diverged from the user-facing wording in the web HUD. | Alternative: keep detailed prototype status wording.
- [UI J.1] Decision: Use web ellipsis punctuation (`…`) for `Spawning asteroids…` in gameplay status copy. | Why: timer caption already used the web string; matching punctuation avoids subtle text drift across HUD surfaces. | Alternative: keep ASCII `...` in gameplay info while timer uses `…`.
- [UI J.2] Decision: Set `Main.tscn` default `GameOverHint` text to `Waves survived: 0`. | Why: runtime populates this label, but matching default copy prevents visible phrasing mismatch before first game-over bind and keeps scene text parity-complete. | Alternative: leave legacy `Wave 0 survived` placeholder.
- [UI J.3] Decision: Update `ResearchHint` copy to web-like skill-tree instructions (toggle, mouse pan, scroll zoom, center-reticle hover). | Why: previous hint documented keys but omitted the interaction model users rely on when navigating research in web UI. | Alternative: keep compact key-only hint.
- [UI J.1/J.3] Decision: Rename gameplay-tail shortcut label from `R panel` to `R research`. | Why: web-facing copy names this surface as research, so `panel` wording introduced terminology drift across HUD hints. | Alternative: keep `R panel` as implementation-centric wording.
- [UI D.4] Decision: Change look-info movement label to `Q/E height` for wording parity with web hints. | Why: `Q/E up-down` was understandable but not text-identical to the web control description users already see elsewhere. | Alternative: keep `up-down` wording as a local phrasing variant.
- [UI J.1/J.3] Decision: Expand HUD shortcut label to `R hero research (with commander)` to match web specificity. | Why: web hint clarifies commander dependency for hero research, while generic `R research` can imply universal availability. | Alternative: keep shorter `R research` phrasing.
- [UI J.3] Decision: Mirror commander qualifier in the in-panel `ResearchHint` (`Press R to toggle hero research (with commander)`). | Why: panel copy should not omit the same commander gating now shown in menu/HUD hints. | Alternative: keep generic `Press R to toggle` panel wording.
- [UI D.4/J.1] Decision: Mirror commander-qualified hero-research wording in `look_readout` shortcut strip. | Why: mixed phrasing across gameplay HUD surfaces (`R research` vs commander-qualified labels) created unnecessary copy inconsistency. | Alternative: keep compact `R research` in look-readout only.
- [INPUT D.4 / UI J.3] Decision: Gate `toggle_research_panel` on commander selection (`selected_commander != "none"`). | Why: web `R` hero research path is unavailable with no commander, so always-open Godot toggle allowed non-parity overlay access. | Alternative: keep input always available and rely only on hint text to imply commander dependency.
- [UI J.3] Decision: Force-close `research_panel_open` inside `_update_research_panel_visibility()` when commander is `none`. | Why: commander changes and phase transitions can leave stale open-state flags; enforcing close at visibility sync keeps panel lifecycle parity-safe. | Alternative: gate input only and leave stale open flags untouched.
- [UI J.1/J.3] Decision: Make gameplay shortcut hints conditional on commander availability, showing `R hero research (with commander)` only when `selected_commander` is set. | Why: once `R` behavior is gated, always showing the shortcut when unavailable is misleading and non-parity from an interaction affordance standpoint. | Alternative: keep static hint text regardless of availability.
- [INPUT D.4] Decision: Close `research_panel_open` before executing build-cycle input (`C`/`B`) during PLAYING. | Why: web control precedence treats build-wheel shortcut as a mode switch out of research context, while background cycling under open research panel is confusing and non-parity. | Alternative: keep cycling active while research overlay remains open.
- [UI J.3] Decision: Drive `ResearchHint` text from commander availability each HUD label refresh. | Why: commander-gated hero research now has conditional behavior and shortcut hints; static in-panel copy can falsely imply hero research availability when commander is `none`. | Alternative: keep one static hint string and rely on hidden panel state alone.
- [UI J.1/J.3] Decision: Build the main-menu controls hint string dynamically and include `R hero research (with commander)` only when commander research is available. | Why: menu guidance should not advertise unavailable hero research controls in no-commander state, mirroring the same commander-aware shortcut affordance now used in gameplay HUD/readout. | Alternative: keep a static menu hint that always mentions `R` hero research.
- [UI J.1/J.3] Decision: Add a dedicated menu commander-status hint line and drive its text from commander availability (`Neutral tech only...` vs commander-selected availability copy). | Why: web main menu explicitly communicates neutral/no-hero state; keeping that context separate from controls improves parity and avoids overloading shortcut text. | Alternative: keep commander state implicit and only mention it in shortcut wording.
- [I.3/I.4 / UI J.3] Decision: Add a menu `Commander` `OptionButton` and bind it directly to `selected_commander` (`none`, `archangel`, `dominion`, `nova`, `citadel`, `jupiter`, `kingpin`) before run start. | Why: web source lets players choose commander pre-run; without any selection UI Godot stayed locked to neutral-only state and could not exercise commander-gated hero research paths. | Alternative: leave `selected_commander` fixed/internal until full hero systems are implemented.
- [UI J.1/J.3] Decision: Keep menu shortcut guidance as web-static copy (`R hero research (with commander)` always visible) and use the separate commander-status hint for availability state. | Why: web menu control text is static and commander-qualified; splitting controls from availability preserves wording parity while still clarifying neutral-vs-commander state. | Alternative: keep menu shortcut line conditional on selected commander.
- [I.3 / UI J.2/J.3] Decision: Centralize commander display labels in `CommanderSystem.display_name()` and route menu/game-over commander text through it. | Why: raw commander ids (`archangel`, etc.) are implementation values, while web UI uses player-facing labels; a shared mapper keeps wording consistent across surfaces. | Alternative: keep ad-hoc per-file capitalization/string formatting.
- [A.2 / I.3] Decision: Add `selected_commander` to `GameState` and mirror it from `main.gd` in `_sync_game_state_runtime()`. | Why: commander choice is now a first-class run/menu control input, and parity-state mirrors should expose it centrally for downstream UI/debug hooks. | Alternative: keep commander only in `main.gd` local state.
- [UI J.3] Decision: Keep `MenuCommanderHint` as static web-equivalent neutral-tech wording instead of switching text when a commander is selected. | Why: web menu presents a fixed “Neutral tech only — no hero buildings or hero research.” note in the commander section; dynamic availability prose is extra behavior not present in source UI copy. | Alternative: keep dynamic commander-selected messaging in this label.
- [VALIDATION] Decision: Continue logging headless parse check as blocked in automation while still running available lint/static checks. | Why: `godot --headless --check-only` cannot execute in this terminal context. | Alternative: skip all validation steps until CLI path is fixed.
